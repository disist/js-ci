language: node_js
node_js:
  - "4.2"

before_script:
  - npm install -g gulp
  - npm install -g bower
  - bower install

  - "export CHROME_BIN=chromium-browser"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

script:
  - npm test

